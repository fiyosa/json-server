{
  "name": "json-server",
  "version": "1.0.0",
  "description": "fake rest api",
  "scripts": {
    "start": "npx json-server db.json -s ./public -p 4000"
  },
  "author": "fys",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-beta.1"
  }
}
