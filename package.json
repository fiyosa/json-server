{
  "name": "json-server",
  "version": "1.0.0",
  "description": "fake rest api",
  "scripts": {
    "dev:ori": "npx json-server --watch db.json -p 4000",
    "dev": "set NODE_ENV=dev && node server.js",
    "start": "node server.js"
  },
  "author": "fys",
  "license": "ISC",
  "dependencies": {
    "canvas": "^2.11.2",
    "json-server": "0.17.4"
  }
}
